Check out the fireball-unpacker into the compilers/ directory.  Then run it
(after you compile it) in Debug.  It will extract all the files in .dat supplied
as an input argument.  You will need to have engine.dll in the directory that
unpacker.exe is in.
